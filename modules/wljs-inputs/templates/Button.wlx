Component[OptionsPattern[]] := With[{
    Event = OptionValue["Event"], 
    Topic = OptionValue["Topic"], 
    Uid = CreateUUID[], 
    Label = OptionValue["Label"],
    Class = OptionValue["Class"],
    Style = OptionValue["Style"]
},
    {
        <button type="button" style="{Style}" id="#instanceId" class="sm-controls rounded wljs-button bg-white px-2 py-1 text-xs text-gray-900 {Class}">
            <Label/>
        </button>
    ,
        <script type="module">
            const el = document.getElementById('#instanceId');
            el.addEventListener('click', (event)=>{
                server.kernel.io.fire('<Event/>', true, '<Topic/>');     
            });
        </script>
    }
];

Options[Component] = {"Event"->"blackhole", "Label"->"Click", "Topic"->"Default", "Class"->"", "Style"->""};

Component
