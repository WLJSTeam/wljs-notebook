%-------------------------------%Notebook%--------------------------------%
ObjectFields:
  - Objects
  - Symbols
  - Storage
  - ExcalidrawImages
  - RuntimeCache
  - ZIPArchive
%-------------------------------------------------------------------------%


%---------------------------------%Cells%---------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
# Interactive evaluation and granular updates

## Manipulate and Animate Functionality
WLJS Notebook partially inherits a well-known killer feature of Wolfram Mathematica - Manipulate, Animate, and others, allowing interactive evaluation of arbitrary expressions:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
# Interactive evaluation and granular updates

## Manipulate and Animate Functionality
WLJS Notebook partially inherits a well-known killer feature of Wolfram Mathematica - Manipulate, Animate, and others, allowing interactive evaluation of arbitrary expressions:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
Manipulate[
  Plot[Sin[a x + b], {x, 0, 6}], 
  {{a, 2, "Frequency"}, 1, 4, 1}, 
  {{b, 0, "Phase"}, 0, 10, 1}, 
  ContinuousAction->True
]

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
Manipulate[Plot[1.0 + Sin[w] Sin[x + w],{x,0,5Pi}, Epilog->{
  Red, Point[{8.0, 1.0 + Sin[w] Sin[8.0 + w]}]
}], {w,0,Pi}, ContinuousAction->True]

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
Text strings or any other symbolic expressions can also be manipulated:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
Text strings or any other symbolic expressions can also be manipulated:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
Manipulate[Column[{
  Style[StringTemplate["`` m/s"][v], Blue],
  Table["🚗", {i, Floor[v/25]}] // Row 
}], {v,10,100}, ContinuousAction->True]

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
Basic 2D/3D primitives can also be manipulated, for example:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
Basic 2D/3D primitives can also be manipulated, for example:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
Manipulate[Graphics[{
  Translate[Rotate[Rectangle[q{-1,-1}, q{1,1}], r], {x, 0}]
}, PlotRange->1], {x,-1,1}, {r,0,Pi/2.0}, {q,0.1,0.2}, ContinuousAction->True]

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
3D plot example:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
3D plot example:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
Manipulate[Plot3D[Sin[n x] Cos[n y], {x,-1,1}, {y,-1,1}], {n, 1, 5, 0.3}, ContinuousAction->True]

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
Images:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
Images:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
img = ImageResize[ExampleData[ExampleData["TestImage"] // Last], 350];
Manipulate[
  ImageAdjust[img, {c,a}], 
  
  {{c, 0},0,5,0.1}, 
  {{a, 0},0,5,0.1},
  ContinuousAction->True
]

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
Under the hood, it uses a diff algorithm to interpret changes between old and new expressions evaluated using corresponding parameters. Then, if possible, the expression is transformed into a more optimized one that uses granular updates instead of full re-evaluation.

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
Under the hood, it uses a diff algorithm to interpret changes between old and new expressions evaluated using corresponding parameters. Then, if possible, the expression is transformed into a more optimized one that uses granular updates instead of full re-evaluation.

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
`Animate` works in a similar way, but scans the parameter automatically:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
`Animate` works in a similar way, but scans the parameter automatically:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
Animate[
  ParametricPlot[ReIm @ Exp[-I (\[Phi] + \[Gamma] I \[Phi])], {\[Phi],0,5Pi}, 
    PlotLabel->StringTemplate["\[Gamma] = ``"][\[Gamma]]
  ]
, {\[Gamma],0,0.5}]

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
For symbolic data it works too:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
For symbolic data it works too:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
Animate[{x, (*SpB[*)Power[x(*|*),(*|*)2](*]SpB*)} // NumberForm, {x,0,10}]

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
Animate[Row[{Sin[x], "==", Series[Sin[x], {x,0,n}], Invisible[1/2]}], {n, 1, 10, 1}, RefreshRate->3]

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
### How to export animation

:::warning
This feature is available only for WLJS desktop application
:::

If you want to export it as GIF, render it to `AnimatedImage` first. For example:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
### How to export animation

:::warning
This feature is available only for WLJS desktop application
:::

If you want to export it as GIF, render it to `AnimatedImage` first. For example:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
w3D[x_,y_,t_] := Total@Table[With[{kx = 1.0 \[Omega], ky = 1.0 \[Omega]}, 
  Cos[ kx x + ky y - \[Omega] t]
], {\[Omega], 0, 3, 1.0}];

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
a = AnimatedImage @ Animate[Plot3D[w3D[x,y,t], {x,-Pi,Pi}, {y,-Pi,Pi}, MaxRecursion->1, Mesh->None, PlotRange->Full], {t,0,5}];

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
Now we can export it as GIF or as a movie:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
Now we can export it as GIF or as a movie:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
Export["animation.gif", a]

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
### More specialized functions
We recommend using `ManipulatePlot` and `ManipulateParametricPlot` for simple interactive curve plotting, since they are more optimized and do not require the diff algorithm to run:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
### More specialized functions
We recommend using `ManipulatePlot` and `ManipulateParametricPlot` for simple interactive curve plotting, since they are more optimized and do not require the diff algorithm to run:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
ManipulatePlot[
  (*TB[*)Sum[(*|*)(*FB[*)((Sin[2π(2j - 1) x])(*,*)/(*,*)(2j-1))(*]FB*)(*|*), {(*|*)j(*|*),(*|*)1.0(*|*),(*|*)n(*|*)}](*|*)(*1:eJxTTMoPSmNiYGAoZgMSwaW5TvkVmYwgPguQCCkqTQUAeAcHBQ==*)(*]TB*)
, {x, -1,1}, {{n,4}, 1,7, 1}]

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
`AnimatePlot` and `AnimateParametricPlot` use a different strategy and precalculate all curves before animating:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
`AnimatePlot` and `AnimateParametricPlot` use a different strategy and precalculate all curves before animating:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
AnimateParametricPlot[
    With[{t = If[x > lim,lim,x]},
      N[Exp[-(*FB[*)((1)(*,*)/(*,*)(3))(*]FB*) Floor[(*FB[*)((x)(*,*)/(*,*)(4 \[Pi]))(*]FB*)]] ReIm[(*SpB[*)Power[I(*|*),(*|*)-t](*]SpB*) + 3 ((*SpB[*)Power[I(*|*),(*|*)t/3](*]SpB*))]]
    ]
    , {x, 0.1`, 16 \[Pi]}, {lim, 0.1`, 16 \[Pi]}
]

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
### Tips for better performance
If you still want to use `Animate` or `Manipulate` on some complex expressions, there are some tricks to avoid deoptimization of JIT engine:

<div class="text-sm mt-2">


- Try to keep the number of curves/traces the same as well as polygons if possible. Try not to add new entities to your graphs.

- `PerformanceGoal->"Speed"` is also a good option for 3D plots.

- `Mesh->None might` also help.

- `PlotRange->Full` can help to avoid fragmentation of line segments

- Avoid changing colors, opacity on runtime

  
</div>

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
### Tips for better performance
If you still want to use `Animate` or `Manipulate` on some complex expressions, there are some tricks to avoid deoptimization of JIT engine:

<div class="text-sm mt-2">


- Try to keep the number of curves/traces the same as well as polygons if possible. Try not to add new entities to your graphs.

- `PerformanceGoal->"Speed"` is also a good option for 3D plots.

- `Mesh->None might` also help.

- `PlotRange->Full` can help to avoid fragmentation of line segments

- Avoid changing colors, opacity on runtime

  
</div>

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
## Granular Updates

The way how you update things like points and lines on your plots, images, or bind those elements to the changes of data differs from one used in Pluto.jl, Marimo and Wolfram Mathematica. If the last one uses 2-ways binding, we rely on 1-way. The key changes were made for the sake of performance and simplicity.

### Architecture

WLJS mostly uses retained mode for managing objects on screen, which means Line or Point has a dedicated method to update itself when the dependencies change. Not all primitives have an update method implemented—please check our symbols reference section to confirm. The dependencies are resolved and the changes are propagated by the frontend (browser), not by the evaluation kernel. To create such a dependency, you need to "offload" an expression to be executed on the frontend. For example:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
## Granular Updates

The way how you update things like points and lines on your plots, images, or bind those elements to the changes of data differs from one used in Pluto.jl, Marimo and Wolfram Mathematica. If the last one uses 2-ways binding, we rely on 1-way. The key changes were made for the sake of performance and simplicity.

### Architecture

WLJS mostly uses retained mode for managing objects on screen, which means Line or Point has a dedicated method to update itself when the dependencies change. Not all primitives have an update method implemented—please check our symbols reference section to confirm. The dependencies are resolved and the changes are propagated by the frontend (browser), not by the evaluation kernel. To create such a dependency, you need to "offload" an expression to be executed on the frontend. For example:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
l = 1;

Graphics[{Cyan,
  (*BB[*)(Rectangle)(*,*)(*"1:eJxTTMoPSmNkYGAoZgESHvk5KRAeB5AILqnMSXXKr0hjgskHleakFnMBGU6JydnpRfmleSlpzDDlQe5Ozvk5+UVFDGDwwb5oDhi8sS+qB4P39gBJMR34"*)(*]BB*)[{-1, -1}, {(*BB[*)(l)(*,*)(*"1:eJxTTMoPSmNkYGAoZgESHvk5KRAeB5AILqnMSXXKr0hjgskHleakFnMBGU6JydnpRfmleSlpzDDlQe5Ozvk5+UVFDGDwwb7o7x8QeGNf9PEDCDywBwB4XSLR"*)(*]BB*) // Offload, 1}]
}]

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
Here we used `Offload` to tell Wolfram Kernel not to evaluate symbol `l` and keep it for evaluating on the frontend. In this example above the binding happens between `Rectangle` and `l`. Nothing stops us from offloading a derived expression from the same symbol:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
Here we used `Offload` to tell Wolfram Kernel not to evaluate symbol `l` and keep it for evaluating on the frontend. In this example above the binding happens between `Rectangle` and `l`. Nothing stops us from offloading a derived expression from the same symbol:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
Graphics[{Pink,
  (*BB[*)(Disk)(*,*)(*"1:eJxTTMoPSmNkYGAoZgESHvk5KRAeB5AILqnMSXXKr0hjgskHleakFnMBGU6JydnpRfmleSlpzDDlQe5Ozvk5+UVFDGDwwb5oDhi8sS+qB4P39gBJMR34"*)(*]BB*)[{(*BB[*)(l)(*,*)(*"1:eJxTTMoPSmNkYGAoZgESHvk5KRAeB5AILqnMSXXKr0hjgskHleakFnMBGU6JydnpRfmleSlpzDDlQe5Ozvk5+UVFDGDwwb7o7x8QeGNf9PEDCDywBwB4XSLR"*)(*]BB*), (*BB[*)(l)(*,*)(*"1:eJxTTMoPSmNkYGAoZgESHvk5KRAeB5AILqnMSXXKr0hjgskHleakFnMBGU6JydnpRfmleSlpzDDlQe5Ozvk5+UVFDGDwwb7o7x8QeGNf9PEDCDywBwB4XSLR"*)(*]BB*)}//Offload, (*SpB[*)Power[(*BB[*)(l)(*,*)(*"1:eJxTTMoPSmNkYGAoZgESHvk5KRAeB5AILqnMSXXKr0hjgskHleakFnMBGU6JydnpRfmleSlpzDDlQe5Ozvk5+UVFDGDwwb7o7x8QeGNf9PEDCDywBwB4XSLR"*)(*]BB*)(*|*),(*|*)2](*]SpB*) // Offload]
}, PlotRange->{{0,1}, {0,1}}]

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
Here the radius of Disk depends on `l^2`, therefore when `l` changes, it recalculates the derived square of this symbol. Now let's try to update our symbol:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
Here the radius of Disk depends on `l^2`, therefore when `l` changes, it recalculates the derived square of this symbol. Now let's try to update our symbol:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
l = 0.33;

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
Using a slider we can observe the change easier:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
Using a slider we can observe the change easier:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
EventHandler[InputRange[0,1,.1], Function[val, l=val]]

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
Here we used a built-in UI element InputRange with a basic event handler assigned to it, that updates our symbol with a new value. When a new value is assigned to `l` symbol, Wolfram Kernel pushes a new value using a fast binary protocol to the frontend, that updates all visible instances coupled to `l`.

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
Here we used a built-in UI element InputRange with a basic event handler assigned to it, that updates our symbol with a new value. When a new value is assigned to `l` symbol, Wolfram Kernel pushes a new value using a fast binary protocol to the frontend, that updates all visible instances coupled to `l`.

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
The provided data does not have to be numerical, it can be textual as well. Here is an example with __text spray__:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
The provided data does not have to be numerical, it can be textual as well. Here is an example with __text spray__:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
Module[{
  text = "Hello World"
},
  Column[{
    EventHandler[InputText[text], (text = #)&],
    Graphics[Table[{
      RandomColor[],
      Rotate[
        Text[text // Offload, RandomReal[{-1, 1}, 2]],
        RandomReal[{0, 3.14}]
      ]
    }, {40}]]
  }]
]

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
### Colors and Opacity
In Wolfram Language color, opacity and other directives are automatically applied to all primitives, even scoped with a List appearing after them, i.e.:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
### Colors and Opacity
In Wolfram Language color, opacity and other directives are automatically applied to all primitives, even scoped with a List appearing after them, i.e.:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
{Red, (*BB[*)(Disk[...])(*,*)(*"1:eJxTTMoPSmNkYGAoZgESHvk5KRAeB5AILqnMSXXKr0hjgskHleakFnMBGU6JydnpRfmleSlpzDDlQe5Ozvk5+UVF8WDw3r5IAAwe2BfNAYM79gBQ7x14"*)(*]BB*), Blue, {(*BB[*)(Disk[...])(*,*)(*"1:eJxTTMoPSmNkYGAoZgESHvk5KRAeB5AILqnMSXXKr0hjgskHleakFnMBGU6JydnpRfmleSlpzDDlQe5Ozvk5+UVF0mBw277o108QeGlfdP8eCLyzBwCCPCLr"*)(*]BB*), (*BB[*)(Line[...])(*,*)(*"1:eJxTTMoPSmNkYGAoZgESHvk5KRAeB5AILqnMSXXKr0hjgskHleakFnMBGU6JydnpRfmleSlpzDDlQe5Ozvk5+UVF0mBw277o108QeGlfdP8eCLyzBwCCPCLr"*)(*]BB*)}}

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
Therefore if we use dynamic symbols and offload them to the frontend, there will be 2 types of binding:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
Therefore if we use dynamic symbols and offload them to the frontend, there will be 2 types of binding:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
opacity = 1.0;
Graphics[{
  Triangle[],
  (*BB[*)(Opacity)(*,*)(*"1:eJxTTMoPSmNkYGAoZgESHvk5KRAeB5AILqnMSXXKr0hjgskHleakFnMBGU6JydnpRfmleSlpzDDlQe5Ozvk5+UVFDGDwwb5oDhi8sS+qB4P39gBJMR34"*)(*]BB*)[(*BB[*)(opacity)(*,*)(*"1:eJxTTMoPSmNkYGAoZgESHvk5KRAeB5AILqnMSXXKr0hjgskHleakFnMBGU6JydnpRfmleSlpzDDlQe5Ozvk5+UVFDGDwwb7o7x8QeGNf9PEDCDywBwB4XSLR"*)(*]BB*) // Offload], 
  Red, (*BB[*)(Disk)(*,*)(*"1:eJxTTMoPSmNkYGAoZgESHvk5KRAeB5AILqnMSXXKr0hjgskHleakFnMBGU6JydnpRfmleSlpzDDlQe5Ozvk5+UVFd26DgX3Rgvkg8N6+6NJFEHhoDwDL+yT7"*)(*]BB*)[{0,0}, 0.25]
}]

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
from opacity to `Opacity` directive, and from `Disk` to `opacity`. When we update `opacity`, the whole chain will be pulled:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
from opacity to `Opacity` directive, and from `Disk` to `opacity`. When we update `opacity`, the whole chain will be pulled:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
opacity = RandomReal[{0,1}];

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
To update `RGBColor` or `Hue` you need to bind them in the same way:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
To update `RGBColor` or `Hue` you need to bind them in the same way:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
color = {1.0,1.0,1.0};
Graphics[{RGBColor[color//Offload], Disk[]}]

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
For convenience, you may apply the following pattern:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
For convenience, you may apply the following pattern:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
color = List@@RGBColor[Pink];

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
color = List@@RGBColor[LABColor[0.6,0.5,0.5]];

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
### Animation
Low-level animation can be done in two ways:

#### As Transitions
This one is reserved exclusively for `Graphics` or higher-order functions like `Plot*` or `HighlightImage`, which produce `Graphics`. Any update to a primitive is interpolated on the frontend by default. You can control it with `TransitionType` and `TransitionDuration` directives, set globally as an option to `Graphics` or locally using `Directive`. Here is an example:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
### Animation
Low-level animation can be done in two ways:

#### As Transitions
This one is reserved exclusively for `Graphics` or higher-order functions like `Plot*` or `HighlightImage`, which produce `Graphics`. Any update to a primitive is interpolated on the frontend by default. You can control it with `TransitionType` and `TransitionDuration` directives, set globally as an option to `Graphics` or locally using `Directive`. Here is an example:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
pos = {-1,0};
Graphics[{
  Directive["TransitionType"->None],
  Disk[(pos+{0,0.5})//Offload, 0.1],
  Directive["TransitionType"->"Linear"],
  Disk[(pos)//Offload, 0.1],
  Directive["TransitionType"->"CubicInOut"],
  Disk[(pos+{0,-0.5})//Offload, 0.1]
}, 
  PlotRange->{{-1,1}, {-1,1}},
  "TransitionDuration"->2000
]

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
Then update the positions instantly:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
Then update the positions instantly:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
pos = -pos;

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
#### Frames
The naive way of animating things could be something like a loop with `Pause`:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
#### Frames
The naive way of animating things could be something like a loop with `Pause`:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
Do[data = animate[i]; Pause[0.01], {i, 99999}]

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
This will work. However, it is a suboptimal approach: it blocks the kernel, it is not in sync with the screen refresh (which may cause flickering), and there is no reliable way to embed it in a presentation or simply suspend it.

To solve these issues for any type of animated graphics, use `AnimationFrameListener`. It does a few things:

<div class="text-sm mt-2">

- It fires an event before the next paint cycle of a window
- It has to be "reloaded" by a change to some symbol for the next shot
- Animation stops if the app is not visible or the parent expression has been removed

These lead to:

- Animation does not block your basic evaluation cycle (async)
- Animation will run as fast as your computation cycle allows and will not spam the computation kernel
- Animation is in sync with your hardware and clamped to the maximum screen refresh rate
- Animation will not run in the background by accident
  
</div>

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
This will work. However, it is a suboptimal approach: it blocks the kernel, it is not in sync with the screen refresh (which may cause flickering), and there is no reliable way to embed it in a presentation or simply suspend it.

To solve these issues for any type of animated graphics, use `AnimationFrameListener`. It does a few things:

<div class="text-sm mt-2">

- It fires an event before the next paint cycle of a window
- It has to be "reloaded" by a change to some symbol for the next shot
- Animation stops if the app is not visible or the parent expression has been removed

These lead to:

- Animation does not block your basic evaluation cycle (async)
- Animation will run as fast as your computation cycle allows and will not spam the computation kernel
- Animation is in sync with your hardware and clamped to the maximum screen refresh rate
- Animation will not run in the background by accident
  
</div>

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
Here is an example with many 3D spheres animated:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
Here is an example with many 3D spheres animated:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
balls = RandomReal[{-1,1}, {100,3}];
vels  = RandomReal[{-1,1}, {100,3}];

Graphics3D[{
  Table[With[{i = i},
    {
      RGBColor[RandomReal[{0,1}, 3]],
      Sphere[balls[[i]] // Offload, 0.03]
    }
  ], {i, Length[balls]}], 

  EventHandler[AnimationFrameListener[balls // Offload], handler]
}]

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
Here `balls` is passed as a dependency to `AnimationFrameListener`, acting as a trigger that signals the animation is complete and needs to fire an event handled by the handler function for the next frame. Let's define our basic handler function that updates the ball positions:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
Here `balls` is passed as a dependency to `AnimationFrameListener`, acting as a trigger that signals the animation is complete and needs to fire an event handled by the handler function for the next frame. Let's define our basic handler function that updates the ball positions:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
handler[_] := With[{},
  vels = Table[
    If[Norm[balls[[i]]] < 0.01, -1, 1] vels[[i]] - 0.08 balls[[i]]
  , {i, Length[balls]}];
  
  balls = Table[balls[[i]] + 0.08 vels[[i]], {i, Length[balls]}];
]

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
To start the animation, you need to evaluate the first cell we described or kick-start it by manually triggering an update:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
To start the animation, you need to evaluate the first cell we described or kick-start it by manually triggering an update:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
balls = balls;

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
To interrupt an animation, you can simply break the cycle:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
To interrupt an animation, you can simply break the cycle:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
handler[_] := Null;

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
Here is another nice and easy example with 2D points:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
Here is another nice and easy example with 2D points:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
Module[{pts = RandomReal[{-1,1}, {100, 2}]},
Graphics[{
  Point[pts // Offload],
  EventHandler[AnimationFrameListener[pts//Offload], Function[Null,
    pts = Map[(# + 0.01 Normalize[{-1,1} #[[{2,1}]]])&, pts];
  ]]
}, "TransitionType"->None, ImageSize->Small]
]

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
In some cases, timers can also be used. If your animation is slow and you do not need to be in sync with the screen, use `SetInterval`. Here is a previous example rewritten with timers:



%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
In some cases, timers can also be used. If your animation is slow and you do not need to be in sync with the screen, use `SetInterval`. Here is a previous example rewritten with timers:



%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
TaskRemove[task] // Quiet;
Module[{
  path = {{0,0}}
},
  task = SetInterval[
    path = Append[path, Last[path] + RandomChoice[{
      {0,1}, {0,-1}, {1,0}, {-1,0}
    }]];
  , 100];
  Graphics[{
    Line[path//Offload]
    }, PlotRange->20{{-1,1},{-1,1}},
    "TransitionType"->None
  ]
]

%------------------------------%EndOfCells%-------------------------------%



%--------------------------------%Objects%--------------------------------%
%-----------------------------%EndOfObjects%------------------------------%



%--------------------------------%Symbols%--------------------------------%
%-----------------------------%EndOfSymbols%------------------------------%



%--------------------------------%Storage%--------------------------------%
%-----------------------------%EndOfStorage%------------------------------%



%---------------------------%ExcalidrawImages%----------------------------%
%-------------------------%EndOfExcalidrawImages%-------------------------%



%-----------------------------%RuntimeCache%------------------------------%
%---------------------------%EndOfRuntimeCache%---------------------------%



%------------------------------%ZIPArchive%-------------------------------%
%----------------------------%EndOfZIPArchive%----------------------------%





%-----------------------------%EndOfNotebook%-----------------------------%