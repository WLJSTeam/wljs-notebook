%-------------------------------%Notebook%--------------------------------%
ObjectFields:
  - Objects
  - Symbols
  - Storage
  - ExcalidrawImages
  - RuntimeCache
  - ZIPArchive
%-------------------------------------------------------------------------%


%---------------------------------%Cells%---------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
# Advanced Slides
This is an advanced guide on how to build live presentations with dynamic content events and complex layouts.

## Data-driven slides
We have seen it already, that one can generate plots from the raw data directly on a slide:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
# Advanced Slides
This is an advanced guide on how to build live presentations with dynamic content events and complex layouts.

## Data-driven slides
We have seen it already, that one can generate plots from the raw data directly on a slide:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
ClearAll[MyFigure];

MyFigure[OptionsPattern[]] := Histogram[RandomVariate[NormalDistribution[0, OptionValue["Sigma"]], 200]];
Options[MyFigure] = {"Sigma" -> 1.0};

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
.slide

<MyFigure Sigma={1.0}/>

%-------------------------------------------------------------------------%
Display: slide
Type: Output
%-------------------------------------------------------------------------%
<dummy >FrontEndExecutable[d5c51f07-b309-402d-ae2e-0bde4050b9d8]</dummy>

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
The same works with any interactive element with `WLXForm` defined (or supported), for example `Manipulate`:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
The same works with any interactive element with `WLXForm` defined (or supported), for example `Manipulate`:

%-------------------------------------------------------------------------%
Props:
  Fade: true
Type: Input
%-------------------------------------------------------------------------%
Cannon = Manipulate[
  Module[{g = 9.81, t, tmax, x, y, vx, vy},
    tmax = (2 * v0 * Sin[angle Degree]) / g;
    
    Show[
      (* Trajectory plot *)
      ParametricPlot[
        {v0 * Cos[angle Degree] * t, 
         v0 * Sin[angle Degree] * t - 0.5 * g * t^2},
        {t, 0, tmax},
        PlotStyle -> {Blue, Thick},
        PlotRange -> {{0, 100}, {0, 60}},
        AspectRatio -> 0.6,
        GridLines -> Automatic,
        FrameLabel -> {"Distance (m)", "Height (m)"},
        ImageSize -> 300
      ],
      Graphics[{
        Red, Thick, 
        Arrow[{{0, 0}, 2.0 {v0 * Cos[angle Degree] / 5.0, v0 * Sin[angle Degree] / 5.0}}]
      }],
      Graphics[{
        Green, PointSize[0.05],
        Point[{v0 * Cos[angle Degree] * tmax, 0}]
      }],
      Epilog -> {
        Text[
          "Range: " <> ToString[N[v0 * Cos[angle Degree] * tmax, 3]] <> " m", {50, 5+30}
        ],
        Text[
          "Max Height: " <> ToString[N[(v0 * Sin[angle Degree])^2 / (2 * g), 3]] <> " m", {50, 10+30}
        ]
      }
    ]
  ],
  
  {{v0, 30, "Initial Velocity (m/s)"}, 5, 35, 5},
  {{angle, 45, "Launch angle Degree (°)"}, 15, 90, 15}, 
  Appearance->None
];

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
.slide

## Projectile Motion Simulator

Adjust the parameters to see how they affect the trajectory:

<Cannon/>

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
However, we can push the idea of slides and data further and bind different actions to the events generated on a slide.

## Events binding
Every slide generates a bunch of events that can be captured with `SlideEventListener`. For example we can capture when the slide becomes visible or invisible:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
However, we can push the idea of slides and data further and bind different actions to the events generated on a slide.

## Events binding
Every slide generates a bunch of events that can be captured with `SlideEventListener`. For example we can capture when the slide becomes visible or invisible:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
EventHandler["generated-by-slide", {
    "Slide" -> Function[Null,
        Print["I am visible"];
    ], 
    "Left" | "Destroy" -> Function[Null,
        Print["I am no longer visible"];
    ]
}];

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
.slide

# First slide 

---

# Second slide

<SlideEventListener Id={"generated-by-slide"}/>

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
The same works with fragments:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
The same works with fragments:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
EventHandler["generated-by-slide", {
    "fragment-1" -> Function[Null,
        Print["I am a fragment 1"];
    ],
    "fragment-2" -> Function[Null,
        Print["I am a fragment 2"];
    ]
}];

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
.slide

# Fragments

I am a fragment 1 <!-- .element: class="fragment" -->

I am a fragment 2 <!-- .element: class="fragment" -->

<SlideEventListener Id={"generated-by-slide"}/>

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
### Stat Counter
Here is an example component of a basic stat display:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
### Stat Counter
Here is an example component of a basic stat display:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
.wlx

Stat[Text_, OptionsPattern[]] := With[{
      Count = OptionValue["Count"]
},

      <div class="text-center text-gray-600 m-4 p-4 rounded bg-gray-100 flex flex-col">
          <Count />
          <span class="text-md"><Text /></span>
      </div>
]

Options[Stat] = {"Count" -> 1};

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
.slide

# Basic counter

<Stat Count={11}>Number of publications</Stat>

%-------------------------------------------------------------------------%
Display: slide
Type: Output
%-------------------------------------------------------------------------%
<dummy >
# Basic counter

<div class="text-center text-gray-600 m-4 p-4 rounded bg-gray-100 flex flex-col">FrontEndExecutable[52ac37fe-5353-4c6b-8a61-a51c06901581]<span class="text-md">Number of publications</span>RVJSEvent["64203b44-12c6-4530-8995-aa27baa77b3e","Slide"]</div></dummy>

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
To make it count up from 0, we need to update the displayed number using a timer that starts when `SlideEventListener` fires the "Slide" event. To display the changing value, we use `HTMLView` (though `TextView` could also work):

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
To make it count up from 0, we need to update the displayed number using a timer that starts when `SlideEventListener` fires the "Slide" event. To display the changing value, we use `HTMLView` (though `TextView` could also work):

%-------------------------------------------------------------------------%
Props:
  Fade: true
Type: Input
%-------------------------------------------------------------------------%
.wlx
Stat[Text_, OptionsPattern[]] := Module[{
      cnt = 0,
      task
}, With[{
      ev = CreateUUID[],
      HTMLCounter = HTMLView[cnt // Offload],
      max = OptionValue["Count"]
},

      EventHandler[ev, {
          "Destroy" -> Function[Null, EventRemove[ev]; If[task["TaskStatus"] === "Running", TaskRemove[task]]; ClearAll[task];],
          "Left" -> Function[Null, cnt = 0],
          "Slide" -> Function[Null, task = SetInterval[If[cnt < max, cnt += 1, TaskRemove[task]], 15]]
      }];

      <div class="text-center text-gray-600 m-4 p-4 rounded bg-gray-100 flex flex-col">
          <HTMLCounter/>
          <span class="text-md"><Text/></span>
          <SlideEventListener Id={ev}/>
      </div>
] ]

Options[Stat] = {"Count" -> 1};

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
Each `Stat` component is generated with its own scope and counts up independently. You can place them on any slide as many as you want:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
Each `Stat` component is generated with its own scope and counts up independently. You can place them on any slide as many as you want:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
.slide
# Stats

<Row ImageSize={{Full, Automatic}} Alignment={Center}>
      <Stat Count={11}>Citations</Stat>
      <Stat Count={110}>Hours</Stat>
      <Stat Count={1010}>Symbols</Stat>
</Row>

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
Each counter is self-contained and responds only when the slide is active.

## Widgets with plots
Let's create another example: a custom widget that reacts to slide fragments. For instance, a reactive chart with a flying disk that moves when a fragment appears. Here we use the `Offload` method to switch between datasets provided in options:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
Each counter is self-contained and responds only when the slide is active.

## Widgets with plots
Let's create another example: a custom widget that reacts to slide fragments. For instance, a reactive chart with a flying disk that moves when a fragment appears. Here we use the `Offload` method to switch between datasets provided in options:

%-------------------------------------------------------------------------%
Props:
  Fade: true
Type: Input
%-------------------------------------------------------------------------%
.wlx
PlotWidget[OptionsPattern[]] := Module[{
  data = OptionValue["DataA"],
  disk = OptionValue["DataA"] // Last
},

  With[{
    Canvas = Graphics[{
      ColorData[97][1], Line[data // Offload],
      ColorData[97][3], Disk[disk // Offload, {0.4,0.05}]
    }, Axes -> True, ImageSize -> 500, PlotRange -> {{-0.2, 1.1 5 Pi}, 1.1 {-1, 1}},
       TransitionDuration -> 500],
    uid = CreateUUID[],
    dataA = OptionValue["DataA"],
    dataB = OptionValue["DataB"]
  },
    EventHandler[uid, {
      "fragment-1" -> Function[Null,
        data = dataB;
        disk = dataB // Last;
      ],
      ("Left" | "Destroy" | "Slide") -> Function[Null,
        data = dataA;
        disk = dataB // First;
      ]
      }];

    <div class="flex flex-col gap-y-2">
      <Canvas />
      <div class="fragment invisible">Invisible text</div>
      <SlideEventListener Id={uid} />
    </div>
  ]
]

Options[PlotWidget] = {"DataA" -> {}, "DataB" -> {}};

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
Generate 2 datasets:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
Generate 2 datasets:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
{dataA, dataB} = {
  Table[{x, Sin[x]}, {x, 0, 5 Pi, 0.1}],
  Table[{x, Tan[x]}, {x, 0, 5 Pi, 0.1}]
};

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
Mount it on a slide:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
Mount it on a slide:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
.slide

# First slide

---

# Second slide

<PlotWidget DataA={dataA} DataB={dataB} />

---

# Third slide

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
## Animation triggers
Another example of using slide events to trigger animation is `Animate`. The simplest example we can think of is:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
## Animation triggers
Another example of using slide events to trigger animation is `Animate`. The simplest example we can think of is:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
Animate[Graphics3D[{
  Black, Arrow[{{0,0,0}, {8Pi,0,0}}],
  Red, Tube[Table[{x, 0, 5 Sin[x-t]}, {x,0,6Pi,0.1}], 0.2],
  Blue, Tube[Table[{x, 5 Sin[x-t+Pi/2], 0}, {x,0,6Pi,0.1}], 0.2]
}], {t,0,16Pi,0.1}, RefreshRate->60]

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
`Animate` supports external controls wired via `EventObject`, which allow you to start, stop, or suspend the animation:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
`Animate` supports external controls wired via `EventObject`, which allow you to start, stop, or suspend the animation:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
.wlx

Animation := With[{
  ev = EventObject[],
  proxy = EventObject[]
}, {
  AnimationHolder = Animate[Graphics3D[{
      Black, Arrow[{{0,0,0}, {8Pi,0,0}}],
      Red, Tube[Table[{x, 0, 5 Sin[x-t]}, {x,0,6Pi,0.1}], 0.2],
      Blue, Tube[Table[{x, 5 Sin[x-t+Pi/2], 0}, {x,0,6Pi,0.1}], 0.2]
  }], {t,0,16Pi,0.1}, RefreshRate->60, "TriggerEvent"->proxy]
},
      EventHandler[ev, {
        "Slide" -> (EventFire[proxy, "Start", Null]&),
        "Left" | "Destroy" -> (EventFire[proxy, "Stop", Null]&)
      }];
      <div>
          <AnimationHolder/>
          <SlideEventListener Id={ev}/>
      </div>
]

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
Here we used proxy to filter out other events and properly wire the rest. Let's place it on a slide:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
Here we used proxy to filter out other events and properly wire the rest. Let's place it on a slide:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
.slide

# Empty slide

---

<Animation/>

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
## Run Javascript

Here is an example of how to trigger confetti with an external JavaScript library. To understand how Wolfram Language integrates with JavaScript, check out these guides:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
## Run Javascript

Here is an example of how to trigger confetti with an external JavaScript library. To understand how Wolfram Language integrates with JavaScript, check out these guides:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
.html
<script src="https://cdn.jsdelivr.net/npm/party-js@latest/bundle/party.min.js"></script>

%-------------------------------------------------------------------------%
Display: html
Type: Output
%-------------------------------------------------------------------------%
<script src="https://cdn.jsdelivr.net/npm/party-js@latest/bundle/party.min.js"></script>

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
Now it is in the global Javascript scope. Define a javascript function to trigger confetti:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
Now it is in the global Javascript scope. Define a javascript function to trigger confetti:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
.js
core.RunFireworks = async (args, env) => {
const id = await interpretate(args[0], env);
party.confetti(document.getElementById(id).parentNode, {
  count: party.variation.range(20, 40),
  size: party.variation.range(0.8, 2.2),
});
}

%-------------------------------------------------------------------------%
Display: js
Type: Output
%-------------------------------------------------------------------------%
core.RunFireworks = async (args, env) => {
const id = await interpretate(args[0], env);
party.confetti(document.getElementById(id).parentNode, {
  count: party.variation.range(20, 40),
  size: party.variation.range(0.8, 2.2),
});
}

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
Here `RunFireworks[id]` accepts one argument: the ID of a DOM element whose parent will receive the confetti fireworks. We can create a dummy DOM element within a WLX component and trigger `RunFireworks` using `FrontSubmit` in response to slide events:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
Here `RunFireworks[id]` accepts one argument: the ID of a DOM element whose parent will receive the confetti fireworks. We can create a dummy DOM element within a WLX component and trigger `RunFireworks` using `FrontSubmit` in response to slide events:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
.wlx
Party := Module[{
  UId = CreateUUID[],
  Ev = CreateUUID[]
},
  EventHandler[Ev, {
      "Slide" -> Function[Null,
          FrontSubmit[RunFireworks[UId]]
      ]
  }];
  <div id="{UId}">
      <SlideEventListener Id={Ev}/>
  </div>
]

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
Now if you place it on any slide, it will launch confetti once the slide has become visible. For example:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
Now if you place it on any slide, it will launch confetti once the slide has become visible. For example:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
.slide

# Let's have

---

# A Party!

<Party/>

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
## Programmatic slides control
Use `FrontSlidesSelected` to control slides programmatically. Note that this is a frontend function (i.e., undefined on the Wolfram Kernel). Execute it with `FrontSubmit` to navigate to a different slide programmatically. For example:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
## Programmatic slides control
Use `FrontSlidesSelected` to control slides programmatically. Note that this is a frontend function (i.e., undefined on the Wolfram Kernel). Execute it with `FrontSubmit` to navigate to a different slide programmatically. For example:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
FrontSlidesSelected["navigateNext", 1] // FrontSubmit

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
This will navigate the first visible presentation to the next slide. You can connect external devices and manipulate slides remotely via Wolfram Language, timers, or buttons:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
This will navigate the first visible presentation to the next slide. You can connect external devices and manipulate slides remotely via Wolfram Language, timers, or buttons:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
NavButton := Button["→", FrontSlidesSelected["navigateNext", 1] // FrontSubmit];

%-------------------------------------------------------------------------%
Props:
  Hidden: true
Type: Input
%-------------------------------------------------------------------------%
.md
Then use it on a slide:

%-------------------------------------------------------------------------%
Display: markdown
Type: Output
%-------------------------------------------------------------------------%
Then use it on a slide:

%-------------------------------------------------------------------------%
Type: Input
%-------------------------------------------------------------------------%
.slide

Current slide

<NavButton/>

---

Next slide

%------------------------------%EndOfCells%-------------------------------%



%--------------------------------%Objects%--------------------------------%
Key: d5c51f07-b309-402d-ae2e-0bde4050b9d8
%-------------------------------------------------------------------------%
<|"Public" -> Hold[Graphics[{{Directive[RGBColor[1, 0.79375, 0.25], EdgeForm[Directive[Thickness[Small], Opacity[0.595], GrayLevel[0]]]], {{}, {Directive[RGBColor[1, 0.79375, 0.25], EdgeForm[Directive[Thickness[Small], Opacity[0.595], GrayLevel[0]]]], {{Tooltip[StatusArea[{Charting`DelayedMouseEffect[Rectangle[{-3.5, 0}, {-3, 1}, "RoundingRadius" -> 0], {"Style", EdgeForm[{GrayLevel[0.5], AbsoluteThickness[1.5], Opacity[0.66]}]}]}, 1], Style[1, {}]]}, {Tooltip[StatusArea[{Charting`DelayedMouseEffect[Rectangle[{-3, 0}, {-2.5, 1}, "RoundingRadius" -> 0], {"Style", EdgeForm[{GrayLevel[0.5], AbsoluteThickness[1.5], Opacity[0.66]}]}]}, 1], Style[1, {}]]}, {Tooltip[StatusArea[{Charting`DelayedMouseEffect[Rectangle[{-2.5, 0}, {-2, 5}, "RoundingRadius" -> 0], {"Style", EdgeForm[{GrayLevel[0.5], AbsoluteThickness[1.5], Opacity[0.66]}]}]}, 5], Style[5, {}]]}, {Tooltip[StatusArea[{Charting`DelayedMouseEffect[Rectangle[{-2, 0}, {-1.5, 7}, "RoundingRadius" -> 0], {"Style", EdgeForm[{GrayLevel[0.5], AbsoluteThickness[1.5], Opacity[0.66]}]}]}, 7], Style[7, {}]]}, {Tooltip[StatusArea[{Charting`DelayedMouseEffect[Rectangle[{-1.5, 0}, {-1, 28}, "RoundingRadius" -> 0], {"Style", EdgeForm[{GrayLevel[0.5], AbsoluteThickness[1.5], Opacity[0.66]}]}]}, 28], Style[28, {}]]}, {Tooltip[StatusArea[{Charting`DelayedMouseEffect[Rectangle[{-1, 0}, {-0.5, 29}, "RoundingRadius" -> 0], {"Style", EdgeForm[{GrayLevel[0.5], AbsoluteThickness[1.5], Opacity[0.66]}]}]}, 29], Style[29, {}]]}, {Tooltip[StatusArea[{Charting`DelayedMouseEffect[Rectangle[{-0.5, 0}, {0, 40}, "RoundingRadius" -> 0], {"Style", EdgeForm[{GrayLevel[0.5], AbsoluteThickness[1.5], Opacity[0.66]}]}]}, 40], Style[40, {}]]}, {Tooltip[StatusArea[{Charting`DelayedMouseEffect[Rectangle[{0, 0}, {0.5, 39}, "RoundingRadius" -> 0], {"Style", EdgeForm[{GrayLevel[0.5], AbsoluteThickness[1.5], Opacity[0.66]}]}]}, 39], Style[39, {}]]}, {Tooltip[StatusArea[{Charting`DelayedMouseEffect[Rectangle[{0.5, 0}, {1, 21}, "RoundingRadius" -> 0], {"Style", EdgeForm[{GrayLevel[0.5], AbsoluteThickness[1.5], Opacity[0.66]}]}]}, 21], Style[21, {}]]}, {Tooltip[StatusArea[{Charting`DelayedMouseEffect[Rectangle[{1, 0}, {1.5, 16}, "RoundingRadius" -> 0], {"Style", EdgeForm[{GrayLevel[0.5], AbsoluteThickness[1.5], Opacity[0.66]}]}]}, 16], Style[16, {}]]}, {Tooltip[StatusArea[{Charting`DelayedMouseEffect[Rectangle[{1.5, 0}, {2, 8}, "RoundingRadius" -> 0], {"Style", EdgeForm[{GrayLevel[0.5], AbsoluteThickness[1.5], Opacity[0.66]}]}]}, 8], Style[8, {}]]}, {Tooltip[StatusArea[{Charting`DelayedMouseEffect[Rectangle[{2, 0}, {2.5, 4}, "RoundingRadius" -> 0], {"Style", EdgeForm[{GrayLevel[0.5], AbsoluteThickness[1.5], Opacity[0.66]}]}]}, 4], Style[4, {}]]}, {Tooltip[StatusArea[{Charting`DelayedMouseEffect[Rectangle[{2.5, 0}, {3, 1}, "RoundingRadius" -> 0], {"Style", EdgeForm[{GrayLevel[0.5], AbsoluteThickness[1.5], Opacity[0.66]}]}]}, 1], Style[1, {}]]}}}, {}, {}}}, {{}, {}, {}, {}, {}, {}, {}, {}, {}, {}, {}, {}, {}}}, {AspectRatio -> GoldenRatio^(-1), Axes -> {True, True}, AxesLabel -> {None, None}, AxesOrigin -> {-3.63, 0}, FrameLabel -> {{None, None}, {None, None}}, FrameTicks -> {{Automatic, Automatic}, {Automatic, Automatic}}, GridLines -> {None, None}, GridLinesStyle -> Directive[GrayLevel[0.5, 0.4]], Method -> {CoordinatesToolOptions -> {"DisplayFunction" -> ({Identity[#1[[1]]], Identity[#1[[2]]]} & ), "CopiedValueFunction" -> ({Identity[#1[[1]]], Identity[#1[[2]]]} & )}, "AxisPadding" -> Scaled[0.02], "ChartDefaultStyle" -> {Directive[RGBColor[1, 0.725, 0], Opacity[0.6]], Directive[RGBColor[0.24, 0.6, 0.8], Opacity[0.6]], Directive[RGBColor[0.455, 0.7, 0.21], Opacity[0.6]], Directive[RGBColor[0.922526, 0.385626, 0.209179], Opacity[0.6]], Directive[RGBColor[0.578, 0.51, 0.85], Opacity[0.6]], Directive[RGBColor[0.772079, 0.431554, 0.102387], Opacity[0.6]], Directive[RGBColor[0.4, 0.64, 1], Opacity[0.6]], Directive[RGBColor[1, 0.75, 0], Opacity[0.6]], Directive[RGBColor[0.8, 0.4, 0.76], Opacity[0.6]], Directive[RGBColor[0.637, 0.65, 0], Opacity[0.6]], Directive[RGBColor[0.915, 0.3325, 0.2125], Opacity[0.6]], Directive[RGBColor[0.40082222609352647, 0.5220066643438841, 0.85], Opacity[0.6]], Directive[RGBColor[0.9728288904374106, 0.621644452187053, 0.07336199581899142], Opacity[0.6]], Directive[RGBColor[0.736782672705901, 0.358, 0.5030266573755369], Opacity[0.6]], Directive[RGBColor[0.28026441037696703, 0.715, 0.4292089322474965], Opacity[0.6]]}, "DefaultBoundaryStyle" -> Automatic, "DefaultChartBaseStyle" -> Directive[RGBColor[1, 0.79375, 0.25], EdgeForm[{GrayLevel[0]}]], "DefaultGraphicsInteraction" -> {"Version" -> 1.2, "TrackMousePosition" -> {True, False}, "Effects" -> {"Highlight" -> {"ratio" -> 2}, "HighlightPoint" -> {"ratio" -> 2}, "Droplines" -> {"freeformCursorMode" -> True, "placement" -> {"x" -> "All", "y" -> "None"}}}}, "DefaultPlotStyle" -> Automatic, "DomainPadding" -> Scaled[0.02], "RangePadding" -> Scaled[0.05]}, PlotRange -> {{-3.5, 3}, {All, All}}, PlotRangePadding -> {{Scaled[0.02], Scaled[0.02]}, {Scaled[0.02], Scaled[0.05]}}, Ticks -> {Automatic, Automatic}}]]|>

%-------------------------------------------------------------------------%
Key: 52ac37fe-5353-4c6b-8a61-a51c06901581
%-------------------------------------------------------------------------%
<|"Public" -> Hold[HTMLView["<div style=\"\" class=\"p-2\" data-type=\"html-string\"></div>", Prolog -> CoffeeLiqueur`Extensions`InputsOutputs`Tools`TemplateProcessor[Association["instanceId" -> CreateUUID[]]], Epilog -> CoffeeLiqueur`Extensions`InputsOutputs`Private`InternalElementUpdate[Offload[cnt$531409], "html-string", "innerHTML"]]]|>

%-----------------------------%EndOfObjects%------------------------------%



%--------------------------------%Symbols%--------------------------------%
Key: cnt$531409
%-------------------------------------------------------------------------%
11

%-----------------------------%EndOfSymbols%------------------------------%



%--------------------------------%Storage%--------------------------------%
%-----------------------------%EndOfStorage%------------------------------%



%---------------------------%ExcalidrawImages%----------------------------%
%-------------------------%EndOfExcalidrawImages%-------------------------%



%-----------------------------%RuntimeCache%------------------------------%
%---------------------------%EndOfRuntimeCache%---------------------------%



%------------------------------%ZIPArchive%-------------------------------%
%----------------------------%EndOfZIPArchive%----------------------------%





%-----------------------------%EndOfNotebook%-----------------------------%